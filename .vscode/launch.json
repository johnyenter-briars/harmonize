{
    "configurations": [
        {
            "name": "Python Debugger: FastAPI",
            "type": "debugpy",
            "request": "launch",
            "module": "uvicorn",
            "cwd": "${workspaceFolder}",
            "args": [
                "harmonize.main:app",
                "--reload",
                "--host",
                "0.0.0.0"
            ],
            "jinja": true
        }
    ]
}